<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.projectmanagement">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.NoActionBar">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".pages.property.NewProperty1"
            android:label="@string/title_activity_new_property1"></activity>
        <activity android:name=".pages.tenant.services.TenantSevices" />
        <activity android:name=".pages.landlord.home.Landlord_Home_Activity" />
        <activity android:name=".pages.login.SignUpActivity" />
        <activity android:name=".pages.property.ViewPropertyDetails" />
        <activity android:name=".pages.property.PropertyValuation" />
        <activity android:name=".pages.property.PropertyMortgage" />
        <activity android:name=".pages.property.EntryPropertyDetails" />
        <activity android:name=".pages.dashboard.Dashboard" />
        <activity android:name=".pages.landlord.home.pages.tenant.TentantsList" />
        <activity android:name=".pages.landlord.home.pages.tenant.AddTenant" />
        <activity android:name=".pages.getstarted.GetStarted3" />
        <activity android:name=".pages.getstarted.GetStarted2" />
        <activity android:name=".pages.getstarted.GetStarted1" />
        <activity android:name=".pages.signup.DetailsActivity" />
        <activity android:name=".pages.login.LoginActivity" />
        <activity android:name=".unused.MainActivity" />
        <activity android:name=".pages.login.splashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".pages.tenant.home.Tenant_Home_Activity" />
    </application>

</manifest>